#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    vector<string> p;
    string s;
    for(int i=0; i<n; ++i) {
        cin >> s;
        p.push_back(s);
    }
    string q;
    cin >> q;
    
    return 0;
} 