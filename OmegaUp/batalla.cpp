#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);

	int n;

	cin >> n;

	int arr[n];
	int brr[n];

	for (int i = 0; i < n; ++i) {

	}

	for (int i = 0; i < n; ++i)
	{	
		
	}

	sort(arr,arr+n);
	sort(brr,brr+n);

	int index0=0, index1=0;

	

	cout << res << '\n';

	
	return 0;
}