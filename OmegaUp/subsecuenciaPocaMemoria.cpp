#include <bits/stdc++.h>

using namespace std;

string ALG_B(int m, int n, string A, string B, string LL ) {
    
}

string ALG_C(int m, int n, string A, string B, string C) {
    if ( n == 0 ) {
        C = "";
    } else if ( m == 1 ) {
        for(int i=0; i<n; ++i) {
            if ( B[i] == A[0] ) {
                C = A[0]
            } else {
                C = "";
            }
        }
    } else {
        int i = m / 2;
        
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    return 0;
}