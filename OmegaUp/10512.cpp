#include <bits/stdc++.h>

using namespace std;

struct nodo {
    int val;
    nodo *atras, *adelante;    
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, q;
    cin >> n >> q;
    
    return 0;
}