#include <iostream>
#include <cmath>
#include <cstring>
#include <string>
#include <map>

using namespace std;

int main()
{
    ios::sync_with_stdio(false); cin.tie(0);

    int n; string s;
    while(cin >> n, n!=0) {
        int dp[2*n][2*n], arr[2][2*n];
        for(int i=0; i<2*n; ++i) {
            cin >> s >> arr[0][i] >> arr[1][i];
        }
        
    }
}