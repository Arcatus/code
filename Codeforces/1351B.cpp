#include <bits/stdc++.h>

using namespace std;
using ll = long long;

#define deb(x) cerr << #x << " = " << x << endl;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    vector<int> v = { 1, 2, 3, 4};
    return 0;
}