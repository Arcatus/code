#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    long long a[n];
    for(int i=0; i<n; ++i) cin >> a[i];
    int cnt = 0;
    for(int i=0; i<n; ++i) {
        
    }

    return 0;
}