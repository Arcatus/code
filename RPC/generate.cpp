#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    /*
    int n = 100000;
    int k = 100000;
    mt19937 generator( time(0) );
    uniform_int_distribution<int> negative(-1000000,1000000);
    uniform_int_distribution<int> positive(0,1000000);
    cout << n << " " << k << '\n';
    for(int i=0; i<n; ++i) {
        cout << negative(generator) << ' ';
        cout << positive(generator) << ' ';
        cout << negative(generator) << '\n';
    }
    */
    cout << "1";
    for(int i=0; i<1000; ++i) {
        cout << "0";
    }
    cout<<'\n';
    return 0;
}