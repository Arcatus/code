{
	num a;
	num b;
	num c;
	num d;
	num[0] e;

	bool f;
	bool g;
	bool h;
	bool i;

	i = f && g;

	f = 2 * 2.1;

	f = true && true;
	g = false;

	h = f || g;

	e[0] = 1.9 * 1;

	a = 1 + 2;
	b = 1 - 3;


	c = a + b + 1;

	d = a - b - 1;

	c = 1.4 + 3.1;
	d = 2.1 - 9.1;

	a = 1 / 3;
	b = 1.2 / 3.5;

	a = c / d;

	b = d / c;
}
